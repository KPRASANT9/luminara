---
name: bridge
description: "Cold start any domain. Zero to EXECUTE."
---

# Bridge: Zero → EXECUTE

1. First signal: `csos substrate=NEW output="first data"` → Calvin creates atom #1
2. Feed 10-20 signals → atoms form, gradient grows
3. `csos ring=NEW detail=cockpit` → check F, gradient, decision
4. When speed > rw → Boyer says EXECUTE → deliver first result
5. Feed result back → self-improvement loop begins

## MCP Binding
Add any MCP server to opencode.json. Fetch → extract numbers → absorb. Same physics, any domain.
