# CSOS Native Build — Complete Summarynn## What Was Deliverednn### 1. Dockerfile with LLVM JITn**Location:** `/Users/prasanthkarlaa/Desktop/Luminara/V12/Dockerfile`nnMulti-stage build with:n- **Stage 1 (builder):** Ubuntu 24.04 + LLVM-18 → builds native C binary with JITn- **Stage 2 (runtime):** Node.js + Python + native binary → full runtimenn```dockerfilen# Stage 1: BuildnFROM ubuntu:24.04 AS buildernRUN apt-get install llvm-18-dev libprotobuf-dev ...nRUN make all  # Builds with LLVM JITnn# Stage 2: RuntimenFROM ubuntu:24.04 AS runtimenCOPY --from=builder /build/csos /usr/local/bin/csosnCMD ["/usr/local/bin/csos", "--http", "4097"]n```nn---nn### 2. Docker Compose with Ingress/Egressn**Location:** `/Users/prasanthkarlaa/Desktop/Luminara/V12/docker-compose.yml`nnServices defined:n| Service | Purpose | Port |n|---------|---------|------|n| **nginx** | Ingress/Egress gateway | 4096→80 |n| **csos-native** | Native C binary + LLVM JIT | 4097 |n| **csos-opencode** | OpenCode agent server | 4096 |n| **watchdog** | Health monitor | - |nn**Persistent volume:** `csos-data` → `./csos` directorynn---nn### 3. nginx Configurationn**Location:** `/Users/prasanthkarlaa/Desktop/Luminara/V12/nginx.conf`nnRoutes:n- `/api/csos/*` → csos-native (4097)n- `/agent/*` → csos-opencode (4096)n- `/ws/*` → csos-opencode (WebSocket)nn---nn### 4. HTTP Server Modes Documentationn**Location:** `.csos/deliveries/latest.md`nn| Mode | Port | Protocol |n|------|------|----------|n| Native HTTP | 4097 | JSON over HTTP |n| OpenCode HTTP | 4096 | REST + WebSocket |n| nginx Gateway | 4096→80 | Proxy |n| Unix Socket | /tmp/csos.sock | Unix domain |nn---nn## How to Usenn### Build & Startn```bashncd /Users/prasanthkarlaa/Desktop/Luminara/V12ndocker compose buildndocker compose up -dn```nn### Testn```bashn# Health checkncurl http://localhost:4096/api/healthnn# Native API (via nginx)ncurl -X POST http://localhost:4096/api/csos/absorb \n  -H "Content-Type: application/json" \n  -d '{"action":"absorb","substrate":"test","output":"42.5"}'nn# Native API (direct)ncurl -X POST http://localhost:4097/api/absorb \n  -H "Content-Type: application/json" \n  -d '{"action":"absorb","substrate":"test","output":"42.5"}'n```nn---nn## Architecturenn```nClient → nginx (port 4096) → ┌→ csos-native (4097) [C + LLVM JIT]n                              │    ↓ physicsn                              └→ csos-opencode (4096) [OpenCode]n                                   ↓n                              core.py (5 equations)n```nn---nn## Deliverables Summarynn| File | Status | Description |n|------|--------|-------------|n| `Dockerfile` | ✅ Created | Multi-stage build with LLVM JIT |n| `docker-compose.yml` | ✅ Created | 4 services: nginx, native, opencode, watchdog |n| `nginx.conf` | ✅ Created | Ingress/Egress routing |n| `latest.md` | ✅ Created | HTTP modes + integration guide |n| Local `./csos` binary | ✅ Verified | 78KB, working, 27/27 tests pass |